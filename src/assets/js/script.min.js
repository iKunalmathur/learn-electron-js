$("#menu-toggle").click(function(e){e.preventDefault(),$("#wrapper").toggleClass("toggled")});
$(function(){

  $.notify("Refreshed", "info");

});
function addnotify(){
  console.log("Notify js Clicked");
  $.notify("Adding", "success");
}
function foo(){
  console.log("Notify js Clicked");
  $.notify("Button clicked", "success");
}
function doNotify() {
  console.log("HTML5 Native Notification js Clicked");
  Notification.requestPermission().then(function (result){
    var myNotification = new Notification('Electron Notification',{
      'body': 'Hi :) , this is native HTML5 notification',
      'icon': 'https://img.icons8.com/bubbles/100/000000/appointment-reminders.png'
    });
  });
}
function doNotifyv2() {
  var body = "notification details comes here";
  var dts = Math.floor(Date.now());
  console.log("HTML5 Noti v2 Clicked");
  Notification.requestPermission().then(function (result){
    var myNotification = new Notification('Title',{
      'body': body,
      'icon': 'https://img.icons8.com/cotton/100/000000/appointment-reminders.png',
      // 'image': 'https://img.icons8.com/cotton/100/000000/appointment-reminders.png',
      // 'timestamp': dts,
    });
  });
}
